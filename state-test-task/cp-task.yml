---
platform: linux

inputs:
- name: task-repo

params:
  STATE_PATH_DIR:
  STATE_FILE_NAME:
run:
  path: bash
  args:
  - "-c"
  - |
     set -eux
     cp task-repo/${STATE_PATH_DIR}/${STATE_FILE_NAME} task-repo/${STATE_FILE_NAME}
