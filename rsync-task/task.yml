---
platform: linux

image_resource:
  type: docker-image
  source: {repository: rjain/buildbox}

params:
  DEBUG:
  SSH_PRIVATE_KEY:
  USER:
  SERVER:

inputs:
  - name: task-repo

run:
  path: task-repo/rsync-task/task.sh
